<%= form_for(@beer) do |f| %>
  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% @beer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
     <%= f.select :style_id,  options_from_collection_for_select(@styles,:id,:style, @beer.style.id) %>
  </div>
  <div class="field">
    <%= f.label :brewery%><br>
    <% if @beer.brewery.nil? %>
        <%select = 0%>
    <%else%>
        <% select = @beer.brewery.id %>
    <%end%>
    <%= f.select :brewery_id , options_from_collection_for_select(@breweries, :id, :name, selected = select) %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
